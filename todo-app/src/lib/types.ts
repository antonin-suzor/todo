export interface TODOElementData {
    id: number,
    title: string,
    done: boolean,
    description: string,
    accountId: number,
}

export interface UserData {
    id: number,
    name: string,
}